# Install Isaac Sim per NVIDIA's instructions. carb / omni / pxr come from it, not pip.
# https://docs.isaacsim.omniverse.nvidia.com/latest/installation/
isaacsim[all,extscache]==5.0.0
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0

anthropic>=0.40
openai>=1.50

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1
opencv-python>=4.8
