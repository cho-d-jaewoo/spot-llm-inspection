# Analysis-only dependencies.
#
# Enough to run tests/smoke_test_v2.py and everything in analysis/ — that is,
# to regenerate every table and figure in the paper from a metrics CSV.
# No Isaac Sim, no GPU, no API keys required.
#
#   pip install -r requirements-analysis.txt

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1
